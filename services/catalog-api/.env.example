# Catalog API — configuração do microserviço
# Copie para `.env` e ajuste se necessário (PM2 já lê pelo ecosystem.config.cjs).

# Endereço de bind. Manter em 127.0.0.1 garante que só o PHP local consegue
# falar com a API (não exposto na internet).
HOST=127.0.0.1
PORT=3001

NODE_ENV=production
